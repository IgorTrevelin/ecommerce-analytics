bundle:
  name: ecommerce_analytics

include:
  - delta_lake/resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-4179197974158107.7.azuredatabricks.net

  prod:
    mode: production
    default: false
    workspace:
      host: https://adb-4179197974158107.7.azuredatabricks.net
    resources:
      jobs:
        delta_lake_pipelines_job:
          name: delta_lake_pipelines_job
          run_as:
            user_name: admin@igortrevelinxavieroutlook.onmicrosoft.com
